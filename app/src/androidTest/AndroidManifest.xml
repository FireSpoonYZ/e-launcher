<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Installed only with the test APK; a real cross-UID sender, never a production hook. -->
        <provider android:name="com.example.launcherprobe.ShareTestProvider"
            android:authorities="com.example.launcherprobe.test.share"
            android:exported="false" android:grantUriPermissions="true" />
        <activity android:name="com.example.launcherprobe.ShareSourceActivity" android:exported="true" />
    </application>
</manifest>
